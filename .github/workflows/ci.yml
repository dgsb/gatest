name: basic CI
on:
  pull_request:

jobs:
  lint-and-test:
    runs-on: ubuntu-latest
    steps:
      - run: echo "job triggered by ${{ github.event_name }}"
      - name: checkout
        uses: actions/checkout@v4
